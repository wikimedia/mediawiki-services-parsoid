// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("parsoid", "for parsoid support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("parsoid", "enable parsoid support", "no");

if (PHP_PARSOID != "no") {
	EXTENSION("parsoid", "parsoid.c");
}

